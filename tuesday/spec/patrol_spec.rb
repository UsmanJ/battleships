require 'patrol'

describe Patrol do
  # xit 'creates ship which has the right size' do
  #   ship = Ship.new(5)
  #   expect(ship.size).to eq(5)
  # end
  #
  # xit 'has a size which is a fixnum' do
  #   ship = Ship.new(4)
  #   expect(ship.size).to be_a(Fixnum)
  # end

  # it 'must add patrol to ship array' do
  #   subject.patrol('A1', 'A2')
  #   expect(subject.ship).to include('A1')
  # end
end
